+++
draft = true
title = 'tools'
weight = 0
+++

También existen una serie de comandos adicionales que se deben usar con frecuencia, generalmente los IDE lo ejecutan constantemente cada vez que guardas un archivo.

Formatear el codigo fuente, forzando a mantener el mismo estilo.

```bash
gofmt -w .
```

```bash
go vet ./...
```

deadcode
